% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brainscore.normality.R
\name{brainscore.normality}
\alias{brainscore.normality}
\title{Assess Normality of Individual Gene Set Scores}
\usage{
brainscore.normality(
  gsScore,
  method = c("ks", "shapiro", "both"),
  alpha = 0.05,
  p_adjust_method = "fdr",
  shapiro_max_n = 5000,
  seed = NULL
)
}
\arguments{
\item{gsScore}{A data frame, matrix, or empirical \code{brainscore()} output list
containing individual gene set scores, with subjects/participants as rows
and gene sets as columns.}

\item{method}{Character string specifying the normality diagnostic method.
Default is \code{"ks"}. Other options are \code{"shapiro"} and \code{"both"}.}

\item{alpha}{Numeric significance threshold used to flag non-normal score
distributions after p-value adjustment. Default is 0.05.}

\item{p_adjust_method}{Character string specifying the method for p-value
adjustment. Default is \code{"fdr"}. See \code{\link[stats:p.adjust]{stats::p.adjust()}} for details.}

\item{shapiro_max_n}{Maximum sample size used for Shapiro-Wilk tests.
\code{stats::shapiro.test} supports at most 5000 observations. Default is 5000.}

\item{seed}{Optional random seed used when subsampling observations for
Shapiro-Wilk tests with more than \code{shapiro_max_n} observations.}
}
\value{
A data frame with one row per gene set and normality diagnostic
columns.
}
\description{
This function reports normality diagnostics for individual-level gene set
scores. The diagnostics are intended to guide downstream model choice and are
not used as automatic exclusion criteria.
}
