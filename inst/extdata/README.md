This directory is intentionally lightweight in BrainEnrich 2.x.

The full curated expression matrices, gene-set collections, and manifest now
live in the companion package `BrainEnrichData`. BrainEnrich resolves resources
from an installed `BrainEnrichData`, a sibling development checkout, or a cache
download from the matching BrainEnrichData release.
